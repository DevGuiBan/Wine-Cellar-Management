CREATE TABLE IF NOT EXIST supplier {
    id INT PRIMARY KEY AUTO_INCREMENT,
    name VARCHAR(255),
    cnpj VARCHAR(255),
    email VARCHAR(255)
    phone_number VARCHAR(255),
    address VARCHAR(255)
}